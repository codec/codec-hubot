# hubot-<%= name %>
description "Hubot instance <%= name %>"

start on runlevel [35]
stop on runlevel [016]
respawn

script
  <%= scope.lookupvar('hubot::params::basedir') %>/<%= name %>/launch.sh
end script
